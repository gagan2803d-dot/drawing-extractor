# 📐 Drawing Dimension Extractor

This tool extracts dimensional data (balloon number, nominal value, tolerance, type C/S, instrument)  
from 2D technical drawings (PDFs) and exports them to Excel.

## 🚀 Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
